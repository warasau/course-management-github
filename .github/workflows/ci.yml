name: Java CI with Maven

on:
  pull_request:
    branches: [ "main" ]

jobs:
  gitleaks-scan:
    name: Gitleaks Secret Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks (full scan)
        run: |
          docker run --rm \
            --workdir=/repo \
            --volume "${PWD}:/repo" \
            ghcr.io/gitleaks/gitleaks:8.24.3 \
            detect --source . --exit-code=2 --verbose

  build:
    runs-on: ubuntu-latest
    needs: gitleaks-scan
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Grant execute permission for mvnw
        run: chmod +x mvnw

      - name: Build with Maven
        run: ./mvnw clean package

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: built-jar
          path: ./target/*.jar

#  semgrep-sast:
#    name: Semgrep Security Scan
#    runs-on: ubuntu-latest
#    needs: build
#    permissions:
#      security-events: write
#      actions: read
#      contents: read
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#
#      - name: Run Semgrep scan
#        run: |
#          docker run --rm -v ${{ github.workspace }}:/src returntocorp/semgrep \
#          semgrep scan --config=auto --sarif --output=semgrep.sarif
#
#      - name: Upload SARIF to GitHub Security Dashboard
#        uses: github/codeql-action/upload-sarif@v4
#        if: always()
#        with:
#          sarif_file: semgrep.sarif
#
#      - name: Save SARIF report as Artifact
#        uses: actions/upload-artifact@v4
#        if: always()
#        with:
#          name: semgrep-report
#          path: semgrep.sarif
#
#  dependency-check:
#    name: OWASP Dependency Check
#    runs-on: ubuntu-latest
#    needs: build
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#
#      - name: Set up JDK 21
#        uses: actions/setup-java@v4
#        with:
#          java-version: '21'
#          distribution: 'temurin'
#          cache: maven
#
#      - name: Grant execute permission for mvnw
#        run: chmod +x mvnw
#
#      - name: Run OWASP Dependency Check
#        run: |
#          touch project-suppression.xml
#          ./mvnw org.owasp:dependency-check-maven:check -Dformat=HTML -DsuppressionFile=project-suppression.xml
#        continue-on-error: true
#
#      - name: Upload SCA Report
#        uses: actions/upload-artifact@v4
#        if: always()
#        with:
#          name: dependency-check-report
#          path: target/dependency-check-report.html
#
#  dast-dynamic:
#    name: DAST Scan (ZAP Docker)
#    runs-on: ubuntu-latest
#    needs: build
#    permissions:
#      contents: read
#      security-events: write
#      actions: write
#      checks: write
#
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#
#      - name: Download Build Artifact
#        uses: actions/download-artifact@v4
#        with:
#          name: built-jar
#          path: target
#
#      - name: Set up JDK 21
#        uses: actions/setup-java@v4
#        with:
#          java-version: '21'
#          distribution: 'temurin'
#
#      - name: Start Application
#        run: |
#          nohup java -jar target/*.jar &
#          sleep 30
#
#      - name: Fix permissions
#        run: chmod -R 777 .
#
#      - name: Create Empty Rules File
#        run: touch zap_rules.tsv && chmod 666 zap_rules.tsv
#
#      - name: Pull ZAP Image
#        run: docker pull ghcr.io/zaproxy/zaproxy:stable
#
#      - name: Run ZAP Baseline Scan
#        run: |
#          docker run --rm --network host --user root \
#            -v $(pwd):/zap/wrk/:rw \
#            ghcr.io/zaproxy/zaproxy:stable \
#            zap-baseline.py -t http://localhost:8080 -r zap-baseline-report.html -I || true
#
#      - name: Upload Baseline Report
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: zap-baseline-report
#          path: zap-baseline-report.html
#
#      - name: Run ZAP API Scan
#        run: |
#          docker run --rm --network host --user root \
#            -v $(pwd):/zap/wrk/:rw \
#            ghcr.io/zaproxy/zaproxy:stable \
#            zap-api-scan.py -t http://localhost:8080/v3/api-docs -f openapi -r zap-api-report.html -I || true
#
#      - name: Upload API Report
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: zap-api-report
#          path: zap-api-report.html